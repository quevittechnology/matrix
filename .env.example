# ====================================================================
# DEPLOYMENT CONFIGURATION - UniversalMatrix
# ====================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git (already in .gitignore)
# ====================================================================

# ====================================================================
# NETWORK RPC ENDPOINTS
# ====================================================================
OPBNB_TESTNET_RPC=https://opbnb-testnet-rpc.bnbchain.org
OPBNB_RPC=https://opbnb-mainnet-rpc.bnbchain.org
BSC_TESTNET_RPC=https://data-seed-prebsc-1-s1.binance.org:8545
BSC_RPC=https://bsc-dataseed1.binance.org

# ====================================================================
# DEPLOYMENT WALLET
# ====================================================================
# Private key of the wallet that will deploy the contract
# This wallet needs BNB for gas fees
PRIVATE_KEY=your_private_key_here

# ====================================================================
# CONTRACT CONFIGURATION
# ====================================================================

# Fee Receiver Address (receives 5% admin fees)
# Can be changed later via setFeeReceiver() by owner
FEE_RECEIVER=your_fee_receiver_address_here

# Owner Address (admin control - RECOMMEND GNOSIS SAFE MULTISIG)
# Has full control over contract configuration
# For production, use a 2-of-3 or 3-of-5 multisig!
OWNER=your_owner_address_here

# Root User Wallet Address (receives fallback payments)
# Special user ID 73928 with unlimited earning potential
# Can be changed later via setRootUserAddress() by owner (DAO multisig)
ROOT_USER_ADDRESS=your_root_user_wallet_address_here

# ====================================================================
# VERIFICATION (Optional)
# ====================================================================
# API key for contract verification on block explorer
BSCSCAN_API_KEY=your_bscscan_api_key_here

# ====================================================================
# DEPLOYMENT NOTES
# ====================================================================
# 1. All addresses above can be changed after deployment via admin functions
# 2. Owner should be a Gnosis Safe multisig for production
# 3. Root user address can be updated through DAO governance
# 4. Contract constructor sets all default values automatically
# 5. See MULTISIG_OWNER_SETUP.md for security best practices
